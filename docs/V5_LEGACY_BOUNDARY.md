# V5 Legacy Boundary

The inherited `outputs/`, `model/` and `website/data/` paths contain the historical V4 package. V5 code does not read them. V5 release surfaces are generated only from `data/public`, `evidence`, `research`, `outputs/v5_*` and CI artifacts. V4.1.3 remains immutable in its historical commit/tag.
