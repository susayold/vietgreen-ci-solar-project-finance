# WORKBOOK_SPEC

Core decision interface is fixed at 22 sheets. The repository stores the sheet map and remote inputs/outputs. A workbook-native artifact must be generated and hash-locked remotely before RECRUITER_READY=TRUE.

Trace: Source ID → Pipeline → Solar → Load/PPA → CAPEX/OPEX → Tax/WC → CFADS → Debt → Coverage → Returns → IC.
