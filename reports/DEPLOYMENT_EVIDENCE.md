# Deployment evidence

This candidate report is a schema placeholder. The recruiter-pages workflow must replace it in the workflow artifact after deployment with the candidate GITHUB_SHA, run ID, Pages URL, live release-meta payload, HTTP route results and Pages artifact identity/digest.

A final release cannot claim deployment proof while this report remains PENDING_LIVE_DEPLOYMENT.
