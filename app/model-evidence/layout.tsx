import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Model & Evidence | VietGreen C&I Solar Project Finance',
  description:
    'Trace VietGreen’s frozen V5.1.3 model, evidence classes, 28-sheet workbook, QA controls, reconciliation, CI artifacts and recruiter-ready claim boundaries.',
};

export default function ModelEvidenceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}


